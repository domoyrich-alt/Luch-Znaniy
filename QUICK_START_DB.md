# 🚀 Быстрый запуск без базы данных

## Проблема
Приложение требует PostgreSQL базу данных для работы.

## ✅ Решение 1: Использовать бесплатный облачный PostgreSQL

### Neon.tech (Рекомендуется - 0.5GB бесплатно)

1. Зайдите на https://neon.tech
2. Нажмите "Sign Up" (можно через GitHub)
3. Создайте новый проект "Luch-Znaniy"
4. Скопируйте "Connection String"
5. Вставьте в `.env` файл:

```env
DATABASE_URL=ваша_строка_подключения_из_neon
```

### Supabase (Альтернатива - 500MB бесплатно)

1. Зайдите на https://supabase.com
2. Создайте новый проект
3. Перейдите в Settings → Database
4. Скопируйте "Connection string" (URI)
5. Вставьте в `.env`

## ✅ Решение 2: Docker PostgreSQL (Локально)

```bash
# Запустить PostgreSQL в Docker
docker run -d \
  --name luch-znaniy-db \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=luch_znaniy \
  -p 5432:5432 \
  postgres:15

# В .env файле
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/luch_znaniy
```

## ✅ Решение 3: Запуск БЕЗ базы данных (упрощенный режим)

Для быстрого тестирования UI можно запустить без БД:

1. Закомментируйте проверку БД в `server/db.ts`:

```typescript
// if (!process.env.DATABASE_URL) {
//   throw new Error("DATABASE_URL must be set");
// }
```

2. Используйте in-memory хранилище (данные не сохраняются)

## 🔧 После настройки БД

```bash
# 1. Примените схему
npm run db:push

# 2. Запустите сервер
npm run server:dev

# 3. В другом терминале запустите Expo
npm run expo:dev

# ИЛИ запустите всё сразу
npm run all:dev
```

## 📱 Для Expo в Codespaces

В `.env` измените:
```env
EXPO_PUBLIC_DOMAIN=<ваш-codespace-url>:5000
EXPO_PUBLIC_API_URL=https://<ваш-codespace-url>:5000
```

Получить URL Codespace: `echo $CODESPACE_NAME`
