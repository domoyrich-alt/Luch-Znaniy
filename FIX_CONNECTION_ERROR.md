# 🔧 Решение проблемы "Ошибка подключения к серверу"

## Проблема
При регистрации в приложении появляется ошибка подключения к серверу.

## ✅ Решение

### В GitHub Codespaces:

#### 1. Сделайте порт 5000 публичным

1. **Откройте панель PORTS** в VS Code:
   - Нажмите `Ctrl+` (или View → Terminal)
   - Выберите вкладку **PORTS** (рядом с TERMINAL)

2. **Найдите порт 5000** в списке

3. **Кликните правой кнопкой** на порт 5000 → **Port Visibility** → **Public**

4. **Проверьте URL**:
   ```
   https://potential-giggle-jjwrwrxp557r2qvww-5000.app.github.dev
   ```

#### 2. Запустите серверы

```bash
cd /workspaces/Luch-Znaniy
bash START_SERVERS.sh
```

Скрипт автоматически:
- ✅ Запустит PostgreSQL (если нужно)
- ✅ Применит миграции БД
- ✅ Запустит Backend сервер на порту 5000
- ✅ Проверит доступность

#### 3. Запустите Expo (в новом терминале)

```bash
npx expo start --tunnel
```

**ВАЖНО:** Дождитесь пока туннель подключится и появится QR код.

#### 4. Проверка

Откройте в браузере:
```
https://potential-giggle-jjwrwrxp557r2qvww-5000.app.github.dev/
```

Должна открыться страница (не 404).

---

### 🖥️ На локальном компьютере (Windows):

Если вы используете Cloudflare туннель как предложили:

#### Окно 1: Запуск сервера
```powershell
cd "C:\Attached-Assets\Attached-Assets"

# Установите DATABASE_URL
$env:DATABASE_URL="postgresql://postgres.ciftnvtmzbtwprnhjuot:Abach2314@aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres"

# Примените миграции
npx drizzle-kit push

# Дождитесь "Changes applied"

# Запустите сервер
$env:NODE_ENV="development"
npx tsx server/index.ts
```

Должно появиться: `express server serving on port 5000`

#### Окно 2: Cloudflare туннель
```powershell
cloudflared tunnel --url http://127.0.0.1:5000
```

**Скопируйте домен** из вывода (без `https://`), например:
```
prints-syracuse-demo-laptop.trycloudflare.com
```

#### Окно 3: Запуск Expo
```powershell
cd "C:\Attached-Assets\Attached-Assets"

# Замените YOUR_CLOUDFLARE_DOMAIN на ваш домен из шага 2
$env:EXPO_PUBLIC_DOMAIN="prints-syracuse-demo-laptop.trycloudflare.com"
$env:EXPO_PUBLIC_API_URL="https://prints-syracuse-demo-laptop.trycloudflare.com"

npx expo start --tunnel --clear
```

---

## 🔍 Диагностика

### Проверка что сервер работает:

```bash
# Локально
curl http://localhost:5000/

# Публично (замените на ваш URL)
curl https://potential-giggle-jjwrwrxp557r2qvww-5000.app.github.dev/
```

### Проверка переменных окружения в приложении:

В любом файле клиента добавьте:
```typescript
console.log('API URL:', process.env.EXPO_PUBLIC_API_URL);
```

### Проверка Docker PostgreSQL:

```bash
docker ps | grep luch-znaniy
# Должен быть статус "Up"
```

---

## 📱 Тестирование регистрации

1. **Откройте приложение** в Expo Go (сканируйте QR)

2. **На экране InviteCode** введите:
   ```
   CLASS10A-2024
   ```

3. **Заполните данные:**
   - Имя: Test
   - Фамилия: User

4. **Нажмите "Войти"**

Если всё работает - вы попадёте в приложение!

---

## ❌ Если не работает:

### Ошибка "Network Error" или "Connection refused":

1. **Порт 5000 не публичный**
   - Проверьте панель PORTS в VS Code
   - Сделайте порт Public

2. **Сервер не запущен**
   ```bash
   ps aux | grep "tsx server"
   # Должен быть процесс
   ```

3. **Неправильный URL в .env**
   ```bash
   cat .env | grep EXPO_PUBLIC
   # Проверьте что URL правильный
   ```

### Ошибка ngrok 503:

- Expo туннель не может подключиться к Metro bundler
- Решение: Используйте `--lan` вместо `--tunnel` если в локальной сети

### Ошибка "Invalid invite code":

- Код не существует в БД
- Решение: Вставьте валидный код через SQL или создайте класс

---

## 🎯 Быстрый старт (одной командой)

```bash
cd /workspaces/Luch-Znaniy && bash START_SERVERS.sh && npx expo start --tunnel
```

После этого:
1. Убедитесь что порт 5000 Public в панели PORTS
2. Отсканируйте QR код
3. Тестируйте!
