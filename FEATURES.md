# Расширение функционала чатов и профиля

## 📱 Обзор изменений

Этот релиз значительно расширяет функционал приложения "Луч Знаний", добавляя полноценную систему чатов в стиле Telegram и развернутые настройки профиля.

## ✨ Новые функции

### Система чатов (Telegram-подобная)

#### Экраны чатов
- **ChatsListScreen** - Список всех чатов с превью, временем, непрочитанными сообщениями
- **PrivateChatScreen** - Полнофункциональный экран личного чата
- **ChatInfoScreen** - Информация о чате, участниках и настройках
- **NewChatScreen** - Создание новых чатов с выбором участников
- **SearchMessagesScreen** - Поиск по сообщениям

#### Функции сообщений
- ↩️ **Ответ на сообщение** - Реплай с превью исходного сообщения
- ➡️ **Пересылка сообщений** - Отправка в другие чаты
- ✏️ **Редактирование** - Изменение своих сообщений
- 🗑️ **Удаление** - Для себя или для всех
- 📋 **Копирование** - Текста сообщения

#### Медиа и файлы
- 📷 **Фото** - Камера и галерея
- 📎 **Файлы** - Документы, PDF
- 🎤 **Голосовые сообщения** - Запись и воспроизведение
- 👀 **Просмотр медиа** - Полноэкранный режим

#### Реакции и взаимодействие
- 😊 **Эмодзи реакции** - ❤️ 👍 😂 😮 😢 🔥
- 📌 **Закрепление** - Важные сообщения и чаты
- ✅✅ **Статусы доставки** - Отправлено, доставлено, прочитано

#### Управление чатом
- 🔍 **Поиск** - По сообщениям в чате
- 🔇 **Mute** - Отключение уведомлений
- 📌 **Pin** - Закрепление чата в списке
- 🗑️ **Очистка истории** - Удаление всех сообщений
- 🚫 **Блокировка** - Пользователей в личных чатах

### Настройки профиля

#### Экраны настроек
- **SettingsScreen** - Главное меню настроек
- **EditProfileScreen** - Редактирование имени и фамилии
- **ChangePasswordScreen** - Смена пароля с валидацией
- **PrivacySettingsScreen** - Приватность и видимость
- **NotificationSettingsScreen** - Все уведомления
- **AppearanceSettingsScreen** - Тема, цвет, размер шрифта
- **BlockedUsersScreen** - Чёрный список
- **AboutScreen** - О приложении

#### Разделы настроек

**🔐 Аккаунт:**
- Редактирование профиля
- Смена пароля
- Привязка email/телефона
- Удаление аккаунта

**🔒 Приватность:**
- Видимость профиля (все / класс / никто)
- Видимость оценок (все / только я / родители)
- Статус онлайн (показывать/скрывать)
- "Был(а) в сети" (показывать/скрывать)
- Чёрный список

**🔔 Уведомления:**
- Push-уведомления
- Звуки и вибрация
- Уведомления о оценках
- Уведомления о домашних заданиях
- Напоминания о событиях
- Email-уведомления

**🎨 Внешний вид:**
- Тема (светлая / тёмная / системная)
- Акцентный цвет (8 вариантов)
- Размер шрифта (маленький / средний / большой)
- Сжатый вид списков

**🌐 Общие:**
- Язык приложения
- Автозагрузка медиа (WiFi / всегда / никогда)
- Очистка кэша
- О приложении
- Поддержка

## 📁 Структура файлов

```
types/
├── chat.ts                     # Типы чатов и сообщений
└── settings.ts                 # Типы настроек

components/
├── chat/
│   ├── MessageBubble.tsx       # Компонент сообщения
│   ├── ChatListItem.tsx        # Элемент списка чатов
│   ├── ReplyPreview.tsx        # Превью ответа
│   ├── ReactionPicker.tsx      # Выбор реакции
│   ├── VoiceRecorder.tsx       # Запись голоса
│   ├── MediaPicker.tsx         # Выбор медиа
│   └── MessageContextMenu.tsx  # Контекстное меню
└── settings/
    ├── SettingsSection.tsx     # Секция настроек
    ├── SettingsItem.tsx        # Элемент настроек
    └── ColorPicker.tsx         # Выбор цвета

screens/
├── chats/
│   ├── ChatsListScreen.tsx     # Список чатов
│   ├── PrivateChatScreen.tsx   # Личный чат
│   ├── ChatInfoScreen.tsx      # Информация о чате
│   ├── NewChatScreen.tsx       # Новый чат
│   └── SearchMessagesScreen.tsx # Поиск сообщений
└── settings/
    ├── SettingsScreen.tsx      # Главные настройки
    ├── EditProfileScreen.tsx   # Редактирование
    ├── ChangePasswordScreen.tsx # Смена пароля
    ├── PrivacySettingsScreen.tsx # Приватность
    ├── NotificationSettingsScreen.tsx # Уведомления
    ├── AppearanceSettingsScreen.tsx # Внешний вид
    ├── BlockedUsersScreen.tsx  # Чёрный список
    └── AboutScreen.tsx         # О приложении

navigation/
└── ChatsStackNavigator.tsx     # Навигация чатов

context/
└── SettingsContext.tsx         # Контекст настроек

hooks/
├── useChat.ts                  # Хук для чатов
├── useSettings.ts              # Хук для настроек
└── useVoiceRecorder.ts         # Хук для записи голоса
```

## 🔧 Технические детали

### Используемые технологии
- **TypeScript** - Строгая типизация
- **React Query** - Управление состоянием и запросами
- **AsyncStorage** - Локальное хранение настроек
- **React Navigation** - Навигация между экранами
- **Expo Vector Icons** - Иконки Feather

### API эндпоинты
```
GET    /api/chats                    # Список чатов
GET    /api/chats/:id/messages       # Сообщения чата
POST   /api/chats/:id/messages       # Отправка сообщения
PUT    /api/messages/:id             # Редактирование сообщения
DELETE /api/messages/:id             # Удаление сообщения
POST   /api/messages/:id/reactions   # Добавление реакции
POST   /api/messages/:id/pin         # Закрепление сообщения
POST   /api/chats/:id/pin            # Закрепление чата
POST   /api/chats/:id/mute           # Отключение уведомлений
```

### Модели данных

#### Chat
```typescript
interface Chat {
  id: number;
  type: 'private' | 'group' | 'class';
  name: string;
  avatar?: string;
  lastMessage?: Message;
  unreadCount: number;
  isPinned: boolean;
  isMuted: boolean;
  participants: ChatParticipant[];
  createdAt: string;
}
```

#### Message
```typescript
interface Message {
  id: number;
  chatId: number;
  senderId: number;
  senderName: string;
  senderAvatar?: string;
  text?: string;
  type: 'text' | 'image' | 'file' | 'voice' | 'system';
  mediaUrl?: string;
  fileName?: string;
  voiceDuration?: number;
  replyTo?: Message;
  forwardedFrom?: { chatName: string; senderName: string };
  reactions: Reaction[];
  isPinned: boolean;
  isEdited: boolean;
  status: 'sending' | 'sent' | 'delivered' | 'read';
  createdAt: string;
  editedAt?: string;
}
```

#### UserSettings
```typescript
interface UserSettings {
  // Приватность
  profileVisibility: 'all' | 'class' | 'none';
  gradesVisibility: 'all' | 'self' | 'parents';
  showOnlineStatus: boolean;
  showLastSeen: boolean;
  
  // Уведомления
  pushEnabled: boolean;
  soundEnabled: boolean;
  vibrationEnabled: boolean;
  gradeNotifications: boolean;
  homeworkNotifications: boolean;
  eventReminders: boolean;
  emailNotifications: boolean;
  
  // Внешний вид
  theme: 'light' | 'dark' | 'system';
  accentColor: string;
  fontSize: 'small' | 'medium' | 'large';
  compactMode: boolean;
  
  // Общие
  language: 'ru' | 'en' | 'kz';
  autoDownloadMedia: 'wifi' | 'always' | 'never';
}
```

## 🎨 UI/UX особенности

- ✅ Использование существующих компонентов (`ThemedText`, `ThemedView`, `Card`, `Button`)
- ✅ Следование цветовой схеме из `@/constants/theme`
- ✅ Поддержка тёмной темы
- ✅ Адаптивный дизайн
- ✅ Плавные анимации и переходы
- ✅ Иконки Feather из `@expo/vector-icons`

## 🚀 Использование

### Навигация к чатам
```typescript
navigation.navigate('ChatsList');
```

### Открытие конкретного чата
```typescript
navigation.navigate('PrivateChat', { chatId: 123 });
```

### Открытие настроек
```typescript
navigation.navigate('Settings');
```

### Использование хука чатов
```typescript
import { useChat } from '@/hooks/useChat';

function ChatScreen() {
  const { messages, sendMessage, addReaction } = useChat(chatId);
  
  const handleSend = () => {
    sendMessage({ chatId, text: 'Привет!' });
  };
  
  return // ...
}
```

### Использование хука настроек
```typescript
import { useSettings } from '@/hooks/useSettings';

function SettingsScreen() {
  const { settings, updateSettings } = useSettings();
  
  const toggleNotifications = () => {
    updateSettings({ pushEnabled: !settings.pushEnabled });
  };
  
  return // ...
}
```

## 📝 Примечания

### Заглушки для реализации
Некоторые функции имеют заглушки и требуют реализации на бэкенде:
- 📷 Реальная загрузка фото/файлов
- 🎤 Запись и воспроизведение голосовых сообщений
- 🔔 Push-уведомления
- 📧 Email-уведомления

### Будущие улучшения
- Группировка сообщений по дате
- Typing индикатор (печатает...)
- Онлайн статус в реальном времени
- Шифрование сообщений
- Голосовые/видео звонки
- Стикеры и GIF

## 🔐 Безопасность

- Настройки хранятся локально с использованием AsyncStorage
- Валидация пароля при смене (минимум 6 символов)
- Подтверждение критических действий (удаление аккаунта)
- Опции приватности для контроля видимости данных

## 📦 Зависимости

Все зависимости уже присутствуют в проекте:
- `@react-navigation/native`
- `@react-navigation/native-stack`
- `@tanstack/react-query`
- `@expo/vector-icons`
- `@react-native-async-storage/async-storage`
- `react-native-safe-area-context`

## 🐛 Известные ограничения

1. Голосовые сообщения требуют интеграции с нативным API записи
2. Загрузка медиа требует интеграции с системой хранения файлов
3. Push-уведомления требуют настройки сервиса уведомлений
4. Некоторые функции используют моковые данные для демонстрации

## 👥 Для разработчиков

### Добавление нового типа сообщений
1. Обновить тип `Message['type']` в `types/chat.ts`
2. Добавить рендеринг в `MessageBubble.tsx`
3. Добавить UI для отправки нового типа

### Добавление новой настройки
1. Обновить интерфейс `UserSettings` в `types/settings.ts`
2. Добавить дефолтное значение в `DEFAULT_SETTINGS`
3. Добавить UI элемент в соответствующий экран настроек

### Расширение навигации
Обновить соответствующий тип ParamList:
```typescript
export type ChatsStackParamList = {
  // ...существующие экраны
  NewScreen: { param: string };
};
```

## 📄 Лицензия

© 2025 Луч Знаний. Все права защищены.
